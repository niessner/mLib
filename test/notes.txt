
most constructors should be made explicit to avoid accidental casts, ex. Vector(int)

UINT should be used most of the time (instead of unsigned int)

mLib.natvis goes in C:\Program Files (x86)\Microsoft Visual Studio 11.0\Common7\Packages\Debugger\Visualizers

missing mLib components (roughly in order of importance):
 -- images
 -- binary input and output file streams w/ compression
 -- parameter files
 -- dense matrices
 -- random number generator
 -- eigen wrapper for dense matrices
 -- rectangles / bounding box (pointNd min/max pairs)
 -- PCA
 -- specialized KD trees for point 2d/3d/4d
 -- geometric primitives (lines, planes, rays, etc.)
 -- triangle-only mesh structure
 -- adjacency-aware mesh structure
 -- remainder of D3D11 objects wrapped
 -- graphs
 -- machine learning
 -- basic algorithms (fourier transform, etc.)
